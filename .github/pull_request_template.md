## What

## Why

## Testing

- [ ] Added or updated a test that failed before this change (RED) and
      passes after it (GREEN) — see [CONTRIBUTING.md](../CONTRIBUTING.md#strict-tdd)
- [ ] `python3 -m unittest discover -s tests -v` passes locally
- [ ] `tests/fixtures/*.json` used in any new/changed contract test are real
      captured herdr responses, anonymized (not hand-written)
